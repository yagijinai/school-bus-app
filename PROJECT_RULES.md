# スクールバス運行管理システム 確定仕様書・開発規約（PROJECT_RULES.md）

本書は、本プロジェクトにおいて合意・実装された確定仕様を明文化し、AIエージェントのコンテキスト圧縮や将来の開発における**仕様の忘却・退行（リグレッション）を恒久的に防止する**ための最上位プロジェクト規約です。今後の全改修タスクにおいて、本ファイル記載のルールを常に遵守してください。

---

## 1. 祝日・運休判定ハイブリッドアーキテクチャ

GoogleカレンダーAPI、内蔵祝日法アルゴリズム、スプレッドシートマスターの3層ハイブリッドで判定を行います。

1. **Google公式祝日カレンダー連携**:
   - バックエンド（`Code.gs`）およびフロントエンドにおいて、Google公式日本の祝日カレンダー（`japanese__ja@holiday.calendar.google.com`）から最新の祝日・振替休日データを自動フェッチ。
2. **内蔵祝日法第3条第3項（国民の休日 / オセロ休日）の完全自動判定**:
   - 前日と翌日の双方が国民の祝日である平日は「国民の休日」として自動判定（例: 2026年9月21日「敬老の日」と2026年9月23日「秋分の日」に挟まれた **2026年9月22日（火）は自動的に「国民の休日（運休）」**）。
   - ハッピーマンデー算出式（`1 + ((1 - firstDay + 7) % 7)`）の正確性を維持し、APIレスポンスの有無にかかわらず内蔵ロジック側でも確実に運休として判定・除外すること。
3. **シート「基本・運休期間」に基づく学校独自運休判定**:
   - **単発の学校休業日**（振替休日・開校記念日・行事休等）の完全適用。
   - **長期休業（夏休み・冬休み・春休み等）の「年非依存（MM/DD〜MM/DD形式）」自動補完**: 年（YYYY）が省略されている場合や過去の年になっていても、現在表示中の該当年を自動補完してシームレスに運休判定を適用。
4. **全校運休判定**:
   - 学校運行時刻表（シート「学校運行時刻表」）において、登校便・下校便がすべて空欄、または「運休」と明記されている日は全校運休日として扱う。

---

## 2. 生徒・兄弟構成（保護者マスター B列〜E列）

1. **最大4名の兄弟対応**:
   - スプレッドシート「保護者」シートの B列〜E列（生徒名1〜4）を完全にパースし、同一世帯で最大4名までの兄弟・姉妹を管理。
2. **タブ表示の動的純化（ハードコードの物理的完全禁止）**:
   - 保護者画面（`ParentDashboard.tsx`）の「対象のお子様」タブでは、**「A-1」「A-2」などの文字をJSXにハードコードすることを一切禁止**する。
   - 兄弟世帯（2名以上）:
     - 第1子: `👦 第1子 {student_name}` （例: `👦 第1子 B-1`）
     - 第2子: `👧 第2子 {student_name}` （例: `👧 第2子 B-2`）
     - 第3子: `👦 第3子 {student_name}`
     - 第4子: `👧 第4子 {student_name}`
   - 一人っ子世帯:
     - `👦 {student_name}` （例: `👦 A-1`）のみを表示し、第2子以降の不要なタブは描画しない。

---

## 3. 生徒・世帯削除機能と管理者画面設計

1. **管理者画面のPC大画面前提レイアウト**:
   - 管理者ダッシュボード（`AdminDashboard.tsx`）は情報量が多いため、PC大画面（`max-w-[1600px]`）を前提に快適な一覧性を確保する。
2. **個別生徒の「✕」削除（左詰め繰り上げロジック）**:
   - 兄弟の中の特定生徒（例: 第1子）のみを削除した場合、後続の生徒（第2子→第1子、第3子→第2子）を左詰めで自動繰り上げし、歯抜け状態を作らない。
3. **「🗑 世帯削除」**:
   - 世帯全体の削除ボタンを用意し、確認ダイアログを経て保護者レコードおよび関連する生徒を安全に一括削除できること。

---

## 4. 保護者画面の表示最適化 ＆ 学校運行時刻表モーダル

1. **予約対象日リストの非表示（除外）仕様**:
   - 祝日、振替休日、国民の休日、長期休業期間、学校設定の全校運休日、および土日は、**予約カレンダー一覧・一括予約の確認リストから日付枠自体を非表示（除外）**とする。
   - 保護者画面には、**「実際にバスが運行され、予約が必要な登校日（平日）」のみが連続して並ぶ**こと。
2. **学校運行時刻表モーダル（`SchoolTimetableModal.tsx`）の月切り替えカプセル化**:
   - モーダル内の表示対象年月ステートはモーダル内部に完全にカプセル化（`useState`）する。
   - 親コンポーネント（`ParentDashboard` 等）の定期データ再取得（ポーリング・再レンダリング）が走っても、ユーザーが「＜」「＞」で切り替えた表示月が**勝手に今月にリセット（2秒戻り等）されないよう制御**する。
   - モーダルを「✕」で閉じて再度開いた時のみ、当月（今月）から始まる設計とする。

---

## 5. キャッシュ制御とデプロイ規約（厳守）

1. **ノーキャッシュ設定の維持**:
   - `vercel.json` および HTMLヘッダーにおいて、`Cache-Control: no-cache, no-store, must-revalidate` を厳格に維持し、ブラウザやCDNによる古いコード・ステートのキャッシュを防止する。
2. **本番デプロイ手順**:
   - `npm run build` による TypeScript 型チェックおよびバンドル検証を必ず通過させる。
   - `npx vercel --prod`（プロジェクトスコープ: `yagijinai-6461s-projects`）を用いて本番環境へ直接デプロイする。
3. **モバイル実機ビジュアル検証**:
   - モバイルビューポート（幅 375px × 高さ 812px）でのビジュアル自己検証（ブラウザサブエージェント等）を必須とする。

---

## 6. プロトタイプ用クイックログイン仕様

1. **保護者ワンタップログイン**:
   - ログイン画面（トップ画面）およびヘッダーの「立場切替」に、テスト用ボタンを常設する：
     - `[ 👨‍👩‍👦 A-1の保護者として入る ]`
     - `[ 👨‍👩‍👦 B-1の保護者として入る ]`
   - クリックにより認証コード入力不要で各家庭のマイページへ即時遷移できること。

---

## 7. データ取得待機画面の「5秒タイムアウト検知」＆ 端末データ復帰フォールバック規約

1. **5秒タイムアウト判定**:
   - `loading === true` の開始から5000msが経過しても応答がない場合に、タイムアウト案内ステート（`showTimeoutFallback: true`）を有効化する。
   - バックグラウンドでの通信待機は強制遮断せず裏で待ち続け、通信が完了した場合は自動で通常画面へ移行する。
2. **端末データ状態に応じた復帰アクション分岐**:
   - **ケースA（端末に前回データあり / 2回目以降）**:
     - メッセージ: 「⚠️ データの取得に時間がかかっています（Googleサーバーの混雑、または電波状況の一時的な不安定）」
     - ボタン: `[ 📱 前回の保存データで開く ]`（タップで待機終了、ローカルデータでダッシュボード表示）
     - ボタン: `[ 🔄 もう一度接続する ]`（最初から再試行）
     - 補足: 「※解消しない場合は、一度アプリを閉じて、もう一度開いてください。」
     - バッジ: キャッシュ利用時は画面最上部に「⚠️ 前回取得データで表示中」を常時表示。
   - **ケースB（端末に前回データなし / 初回起動・キャッシュクリア状態）**:
     - メッセージ: 「⚠️ データの取得に時間がかかっています（Googleサーバーの混雑、または電波状況の一時的な不安定）」
     - ボタン: `[ 🔄 もう一度接続する ]` のみ表示（「前回の保存データで開く」は非表示）。
     - 案内: 「※一度アプリを閉じて、もう一度アプリを開いてください。」

