# スクールバス運行管理システム 確定仕様書・最上位開発規約（PROJECT_RULES.md）

本書は、本プロジェクトにおいて合意・実装された全確定仕様と過去の全注文履歴を明文化し、AIエージェントのコンテキスト圧縮や将来の開発における**仕様の忘却・機能退行（デグレ）・無断単純化を恒久的に防止する**ための最上位プロジェクト規約です。

**すべてのセッション・すべてのプロンプトにおいて、コードの変更や提案を行う前に必ず本書を精読し、以下の「7大コア要件」を100%遵守してください。**

---

## 🚨 【最重要】絶対遵守・不可逆チェックリスト（7大コア要件）

改修・機能追加を行う際は、以下の7大コア要件が1つたりとも破壊・後退していないことを必ず自己検証してください。

### ① Googleカレンダー連携 ＆ 日本祝日ハイブリッド判定
- **Google公式日本の祝日カレンダーAPI連携**: バックエンド（`gas/Code.gs`）およびフロントエンドにおいて、公式カレンダー（`japanese__ja@holiday.calendar.google.com`）から最新の祝日・振替休日データを自動フェッチ。
- **内蔵祝日法第3条第3項（国民の休日 / オセロ休日）完全自動判定**:
  - 前日と翌日の双方が国民の祝日である平日は自動的に「国民の休日」として判定。
  - **具体例**: 2026年9月21日（敬老の日）と2026年9月23日（秋分の日）に挟まれた **2026年9月22日（火）は自動的に「国民の休日（運休）」** と判定。ハッピーマンデー算出式（`1 + ((1 - firstDay + 7) % 7)`）の正確性を恒久維持。
- **シート「基本・運休期間」の単発/MM/DD長期休業自動判定**:
  - 単発の学校休業日（振替休日・開校記念日・行事休等）の完全適用。
  - 長期休業（夏休み・冬休み・春休み等）における **「年非依存（MM/DD〜MM/DD形式）」** の該当年自動補完。
- **保護者予約画面での完全除外・非表示**:
  - 祝日、振替休日、国民の休日、長期休業期間、学校設定の全校運休日、土日は、**予約カレンダー一覧および一括予約確認リストから日付枠自体を完全除外（非表示）**。
  - 保護者画面には、**「実際にバスが運行され、予約が必要な登校日（平日）」のみが連続して並ぶ**こと。

### ② 生徒・兄弟構成の完全動的化（ハードコードの永久禁止）
- **スプレッドシート「生徒・保護者マスター」B〜E列（最大4名兄弟対応）**:
  - 同一世帯で最大4名までの兄弟・姉妹を完全サポート。
- **「A-1」「A-2」等の固定文字の物理的完全禁止**:
  - JSXやTypeScriptコード内に「A-1」「B-1」などの固定テキストを混入させることは一切禁止。
  - 一人っ子世帯は `👦 {student_name}` のみ表示。第2子以降の不要なタブは描画しない。
  - 兄弟世帯は `👦 第1子 {student_name}`、`👧 第2子 {student_name}` ... と動的に描画。
- **ログイン画面・立場切替のワンタップボタン完全動的化 (`householdUtils.ts`)**:
  - ログイン画面（`LoginPage.tsx`）および立場切替（`RoleSwitcher.tsx`）のプロトタイプボタンは、スプレッドシートから読み込んだ先頭世帯（例: 山田様世帯、佐藤様世帯）から自動生成。
  - メインラベル: `👨‍👩‍👦 {苗字}様の保護者として入る`（例: `👨‍👩‍👦 山田様の保護者として入る`）
  - サブラベル: `{生徒名1}・{生徒名2} 世帯`（例: `山田　花子・山田　花男 世帯`）
  - マスター未取得時は安全なフォールバック（`保護者1として入る` 等）を適用。

### ③ 学校運行時刻表モーダルの月ステート独立カプセル化
- **月切り替えステートの完全カプセル化 (`SchoolTimetableModal.tsx`)**:
  - モーダル内の表示年月ステート（`viewYear`, `viewMonth`）はモーダル内部に完全に独立カプセル化（`useState`）。
  - 親コンポーネント（`ParentDashboard` 等）のバックグラウンドフェッチや親の再レンダリングが走っても、ユーザーが「＜」「＞」で切り替えた表示月が**勝手に初期化（今月戻り / 2秒戻り）されないよう厳格に遮断**。
  - モーダルを閉じて再度開いた時のみ、当月（今月）から始まる設計を厳守。

### ④ 通信遅延時の安全保護（5秒タイムアウト判定・復帰フォールバック）
- **5秒タイムアウト判定 (`AppContext.tsx` / `App.tsx`)**:
  - `loading === true` の開始から5000msが経過しても応答がない場合に、タイムアウト案内ステート（`showTimeoutFallback: true`）を有効化。
  - バックグラウンドでの通信待機は強制遮断せず裏で待ち続け、通信が完了した場合は自動で通常画面へ移行。
- **端末データ状態に応じた動的復帰アクション**:
  - **ケースA（端末に前回データあり）**:
    - `[ 📱 前回の保存データで開く ]`（ローカルデータで即座にダッシュボード表示）
    - `[ 🔄 もう一度接続する ]`（再試行）
    - `[ 🗑 保存データを全消去して再同期 ]`（キャッシュ完全クリア＆GAS強制再取得）
  - **ケースB（端末に前回データなし / 初回・全消去後）**:
    - `[ 🔄 もう一度接続する ]`
    - `[ 🗑 保存データを全消去して再同期 ]`

### ⑤ SWR高速化 ＆ GAS CacheService ＆ 強制クリア・再同期
- **Stale-While-Revalidate（体感0秒起動）**:
  - 端末にキャッシュがある場合は即座に画面描画を開始し、裏でGASから最新データを取得・反映。
- **GAS側 CacheService 高速化（TTL: 30分）**:
  - GAS側で各シートのデータをScriptCacheにキャッシュし、高速レスポンスを実現。
- **強制最新同期 ＆ キャッシュ完全消去機能の常設**:
  - 管理者画面ヘッダー: **「🔄 GASから強制再同期」**（GASキャッシュを破棄し生スプレッドシートから読み出し）
  - 管理者画面ヘッダー / ログイン画面 / 立場切替: **「🗑 保存データを全消去して再同期」**（ブラウザlocalStorageを完全破棄し、GAS ScriptCacheもバイパスして生スプレッドシートから強制再取得）
  - アプリ起動時（初回ロード時）は常に `refreshAll(true)`（force=true）を実行し、古いGASキャッシュによるロールバックを防止。

### ⑥ 管理者画面PC大画面最適化 ＆ 生徒・世帯削除
- **PC大画面前提レイアウト (`max-w-[1600px]`)**:
  - 管理者ダッシュボード（`AdminDashboard.tsx`）はPC大画面での快適な運用を前提に設計。
- **個別生徒の「✕」削除（左詰め繰り上げロジック）**:
  - 兄弟の中の特定生徒（例: 第1子）のみを削除した場合、後続の生徒（第2子→第1子、第3子→第2子）を左詰めで自動繰り上げし、歯抜け状態を作らない。
- **「🗑 世帯削除」**:
  - 世帯全体の削除ボタンを用意し、確認ダイアログを経て保護者レコードおよび関連生徒を一括削除。

### ⑦ Vercelノーキャッシュ配信 ＆ 本番デプロイ規約
- **ノーキャッシュ配信ヘッダー**:
  - `vercel.json` および HTMLヘッダーにおいて、`Cache-Control: no-cache, no-store, must-revalidate` を厳格に維持。
- **本番クリーンデプロイ手順（必須プロトコル）**:
  1. `npm run build` による TypeScript 型チェックおよびバンドル検証を必ず通過。
  2. `git add .`、`git commit`、`git push origin main` を実行。
  3. `npx vercel build --yes --prod` によりローカルでクリーンビルド。
  4. `npx vercel deploy --prebuilt --prod` により本番環境（エイリアス: `https://school-bus-app-sigma.vercel.app`）へ確実にデプロイ。
- **モバイル実機ビジュアル検証（厳守）**:
  - モバイルビューポート（幅 375px × 高さ 812px）でのビジュアル自己検証（ブラウザサブエージェント等）を必須とする。

---

## 2. スプレッドシート全6シート完全定義

スプレッドシート「生徒・保護者マスタ」の各列定義は以下の通りです。バックエンドおよびフロントエンドのパーサーはこれらに厳密に準拠すること。

1. **「生徒・保護者マスター」**:
   - A列: 保護者メールアドレス
   - B列: 生徒名１
   - C列: 生徒名２
   - D列: 生徒名３
   - E列: 生徒名４
   - F列: 登録バス停名
   - G列: 備考
   - H列: 基本_登校（「乗る」、もしくは空白）
   - I列: 基本_下校（「1便」、「2便」、「乗らない」のいずれか）
   - J列: 認証コード（例: `SB-1252`）

2. **「バス停マスタ」**:
   - A列: バス停名
   - B列: 住所
   - C列: 到着予定時刻（登校便）
   - D列: 停車順序

3. **「運行予定カレンダー」**:
   - A列: ID（行番号から1を引いた通し番号数値）
   - B列: 日付（`YYYY/MM/DD` 形式）
   - C列: 生徒名
   - D列: 登校ステータス（「乗る」、もしくは空白）
   - E列: 下校ステータス（乗らない場合のみ「乗らない」、下校便に乗る場合は空白）
   - F列: 下校1便（選択時の時刻文字列、または空白）
   - G列: 下校2便（選択時の時刻文字列、または空白）
   - H列: 下校3便（選択時の時刻文字列、または空白）
   - I列: 備考
   - J列: 更新日時（`YYYY/MM/DD HH:mm:ss`）
   - K列: 保護者メールアドレス

4. **「基本設定・運休期間」**（旧: 基本・運休期間）:
   - A列: 設定名（例: `夏休み`, `開校記念日`, `時刻表公開_2026/09`）
   - B列: 開始日（`YYYY/MM/DD` または `MM/DD`）
   - C列: 終了日（`YYYY/MM/DD` または `MM/DD`）
   - D列: 標準運行（「公開」「非公開」等）
   - E列: 内容・時刻（「確定済」「未確定」等）
   - F列: 備考（「予約受付中」「時刻表調整中・ロック」等）

5. **「学校用時刻表」**:
   - A列: 日付（`YYYY/MM/DD`）
   - B列: 登校便
   - C列: 下校1便
   - D列: 下校2便
   - E列: 下校3便
   - F列: 備考
   - G列: カレンダー表示用

6. **「ユーザー権限マスタ」**:
   - A列: メールアドレス
   - B列: 氏名
   - C列: 役割（管理者 / 運転手 / 保護者 等）

---

## 3. GAS接続先エンドポイント（恒久固定）
- **Web App URL (exec)**:
  `https://script.google.com/macros/s/AKfycbxm4XlGSbamPsbQyKmqg5ia5pJ85LPmgX83Sn-RhNV3gdOcwZpvMB2Oju3z41EBk-6omQ/exec`
  （`.env`, `.env.local`, `.env.production`, `spreadsheetApi.ts` のすべてで常にこのURLを参照すること）
