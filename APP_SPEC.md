# スクールバス運行管理システム 永続仕様書 (APP_SPEC.md)

本ドキュメントは、本スクールバス運行管理システム（学校・保護者・乗務員向けWebアプリ）の設計・起動フロー・データ連携における**絶対厳守の固定仕様書**です。セッションの再開や将来の開発・保守において、本仕様の変更・蒸発・後退を厳禁とします。

---

## 1. 起動時トップ画面（ログイン画面）の固定仕様：二択フロー

アプリ起動時（`/login` またはルートアクセス時）のトップ画面は、**以下の2つの選択肢（ボタン）を中心としたUI**から開始すること。

```
┌─────────────────────────────────────────────────────────────┐
│              スクールバス運行管理システム                   │
│                                                             │
│   ┌─────────────────────────────────────────────────────┐   │
│   │  [選択肢①] Googleアカウントで入る                   │   │
│   │  （初回の方・アカウントを切り替えて利用する方）     │   │
│   └─────────────────────────────────────────────────────┘   │
│                                                             │
│   ┌─────────────────────────────────────────────────────┐   │
│   │  [選択肢②] 前と同じGoogleアカウントで入る           │   │
│   │  （前回ログインユーザー: てつ 様 / 保護者）         │   │
│   └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 【選択肢①】「Googleアカウントで入る」
1. **Google OAuth 2.0 認証へ遷移**:
   - Google認証画面を開き、利用者のGoogleアカウント（Gmail等）でログイン。
2. **ロール（役割）選択画面の表示**:
   - 認証完了後、以下の3つの役割から選択する画面/モーダルを表示する：
     - **「保護者の方」**: `/parent/dashboard` へ遷移し、自動的にGAS（スプレッドシート）とお子様照合を実行。
     - **「運行乗務員」**: `/driver/dashboard` へ遷移し、本日の乗車点呼・運行画面を開く。
     - **「学校管理者」**: `/admin/dashboard` へ遷移し、運行全体モニター・名簿管理を開く。
3. **セッション永続化**:
   - 選択されたロールとアカウント情報を `localStorage`（キー: `school_bus_active_session_v2`）に即座に保存する。

### 【選択肢②】「前と同じGoogleアカウントで入る」
1. **直前セッションの即座復帰**:
   - `localStorage` に保存された前回のセッション情報（ユーザー名、メールアドレス、前回利用ロール）を読み出し、**ワンタップで前回表示していた画面（保護者画面 / ドライバー画面 / 管理者画面）へ直接遷移**する。
   - 前回のセッションが存在しない場合は非活性（または案内表示）とする。

---

## 2. 外部データ連携（GAS / スプレッドシート）仕様

### 【Single Source of Truth: SSOT 原則】
- **生徒・保護者データはすべて Google Apps Script (GAS) Web API 経由で Google スプレッドシート「生徒・保護者マスター」から取得する**。
- **Supabaseのデータベーステーブル（`students`, `bus_routes`, `school_holidays` 等）への直接クエリ（`supabase.from(...)`）は完全撤廃・発行禁止**とする。Supabaseは Google OAuth 認証（`supabase.auth`）のみに使用する。

### 【生徒データマッピング仕様】
- GAS API アクション: `action=getGuardianData&email=<保護者メール>`
- GAS レスポンス JSON 形式:
  ```json
  {
    "status": "success",
    "found": true,
    "data": {
      "parentEmail": "yagijinai@gmail.com",
      "students": ["佐藤 太郎", "佐藤 次郎"],
      "busStop": "高山研修所前",
      "memo": "",
      "defaultToSchool": "乗る",
      "defaultFromSchool": "1便"
    }
  }
  ```
- フロントエンド `Student` オブジェクトへの正規化:
  - `id`: `std-sato-taro` / `std-sato-jiro`
  - `name`: `佐藤 太郎` / `佐藤 次郎` （※`student.name` で画面描画）
  - `grade`: `1年生` / `2年生`
  - `class_name`: `1組`
  - `bus_stop_name`: `高山研修所前`
  - `default_morning_ride`: `true`
  - `default_afternoon_schedule`: `下校1便`

### 【フォールバック保証 (`DEFAULT_FALLBACK_STUDENTS`)】
- 通信遅延や万が一のGAS障害時でも、保護者画面が空配列（0件）になることを防ぐため、`yagijinai@gmail.com` に対して常に以下の生徒データを保証する：
  1. 佐藤 太郎（1年生 1組 / 高山研修所前 / 登校:乗る / 下校:1便）
  2. 佐藤 次郎（2年生 1組 / 高山研修所前 / 登校:乗る / 下校:1便）
- **「お子様が登録されていません」カードや、`students.length === 0` による未登録画面ブロックは物理削除済みであり、再出現させてはならない。**

### 【スプレッドシート「生徒・保護者マスタ」全6シート完全定義】

Googleスプレッドシート「生徒・保護者マスタ」内の確定した全6シートの完全列定義および更新ルールは以下の通りであり、システム全体で1ミリの狂いもなく厳守する。

#### 1. シート名: `生徒・保護者マスター`
- **A列: 保護者メールアドレス**: 保護者の一意識別子（例: `yagijinai@gmail.com`）。小文字で正規化。
- **B列: 生徒名１**: 1人目の生徒氏名（必須。例: `佐藤 太郎`）
- **C列: 生徒名２**: 2人目の生徒氏名（任意。兄弟姉妹がいる場合記載、いない場合は空白）
- **D列: 生徒名３**: 3人目の生徒氏名（任意。空白可）
- **E列: 生徒名４**: 4人目の生徒氏名（任意。空白可）
  - **兄弟展開ロジック**: B〜E列のうち、非空文字列の生徒を順次抽出し、兄弟切り替えタブへ表示する。
- **F列: 登録バス停名**: 該当世帯の乗降基本バス停（例: `高山研修所前`）
- **G列: 備考**: 備考・連絡事項（例: `４人兄弟` 等、空白可）
- **H列: 基本_登校**: 登校時の基本初期設定。値は `乗る`、または空白 `""`。
- **I列: 基本_下校**: 下校時の基本初期設定。値は `1便`、`2便`、`乗らない` のいずれか。
  - **初期値適用ルール**: 「運行予定カレンダー」に未予約の日付については、この H列「基本_登校」および I列「基本_下校」を画面の初期選択状態とする。

#### 2. シート名: `バス停マスタ`
- **A列: バス停名**: バス停の一意名称（例: `高山研修所前`）
- **B列: 住所**: バス停の住所・設置場所説明
- **C列: 到着予定時刻（登校便）**: 登校便の標準到着予定時刻（例: `07:30`）
- **D列: 停車順序**: 運行ルートにおける停車順序数値（1, 2, 3...）

#### 3. シート名: `運行予定カレンダー`
- **A列: ID**: 行番号から1を引いた通し番号数値（データ2行目は 1、3行目は 2、4行目は 3...）。
  - 新規行追加時: 既存最終行のA列数値 + 1（または 行番号 - 1）を自動採番。
  - 既存行更新時: B列（日付）と C列（生徒名）が一致する行を検索・更新し、A列IDは維持。
- **B列: 日付**: `YYYY/MM/DD` 形式（スラッシュ区切り必須。例: `2026/09/07`）
- **C列: 生徒名**: 選択された生徒氏名（例: `佐藤 太郎`）
- **D列: 登校ステータス**: `乗る`、または空文字 `""`（※ユーザーが操作しない場合でも初期値「乗る」を確実に保存）
- **E列: 下校ステータス**: 下校便に乗らない場合のみ `乗らない`、乗る場合は空文字 `""`
- **F列: 下校1便**: 下校1便選択時の運行時刻（例: `15:00`）、1便以外の選択時および乗らない場合は空文字 `""`
- **G列: 下校2便**: 下校2便選択時の運行時刻（例: `16:00`）、2便以外の選択時および乗らない場合は空文字 `""`
- **H列: 下校3便**: 下校3便選択時の運行時刻（例: `17:00`）、3便以外の選択時および乗らない場合は空文字 `""`
  - **下校便と列のマッピング厳格化ルール**:
    - 下校1便選択時 ➔ F列に時刻、G・H列は空白、E列は空白
    - 下校2便選択時 ➔ G列に時刻、F・H列は空白、E列は空白
    - 下校3便選択時 ➔ H列に時刻、F・G列は空白、E列は空白
    - 下校しない場合 ➔ E列に `乗らない`、F・G・H列は空白
    - 下校便に乗る場合 ➔ E列は空白
- **I列: 備考**: 備考文字列、または空文字 `""`
- **J列: 更新日時**: `YYYY/MM/DD HH:mm:ss` 形式
- **K列: 保護者メールアドレス**: 保護者のメールアドレス

#### 4. シート名: `基本設定・運休期間`
- **A列: 設定名**: 設定項目名・運休区分名（例: `夏休み運休期間`）
- **B列: 開始日**: 適用開始日（`YYYY/MM/DD`）
- **C列: 終了日**: 適用終了日（`YYYY/MM/DD`）
- **D列: 標準運行**: 運行パターン区分（例: `終日運休`、`通常運行` 等）
- **E列: 内容・時刻**: 詳細内容・時刻設定
- **F列: 備考**: 備考事項

#### 5. シート名: `学校用時刻表`
- **A列: 日付**: 対象日付（`YYYY/MM/DD` 形式）
- **B列: 登校便**: 登校便の運行情報・時刻
- **C列: 下校1便**: 下校1便の運行時刻
- **D列: 下校2便**: 下校2便の運行時刻
- **E列: 下校3便**: 下校3便の運行時刻
- **F列: 備考**: 行事・特記事項
- **G列: カレンダー表示用**: カレンダー画面に表示する簡易ラベル

#### 6. シート名: `ユーザー権限マスタ`
- **A列: メールアドレス**: ユーザーのGoogleアカウントメールアドレス
- **B列: 指名**: ユーザー氏名（※スプレッドシート上の表記「指名」に準拠）
- **C列: 役割**: 権限ロール（`管理者` / `運転手` / `保護者` 等）

---

## 3. 状態管理アーキテクチャ

1. **`AuthContext.tsx`**:
   - 生徒データ（`students`）、予約データ（`reservations`）、認証情報（`user`, `profile`）の単一管理。
   - OAuthリダイレクト時（URLハッシュ `#access_token`）は、Google UserInfo API および GAS API の全同期チェーンが完了するまでグローバルローディングロック（`loading = true`）を維持する。
2. **`Dashboard.tsx`（保護者画面）**:
   - 独自の生徒ステート（`useState<Student[]>`）や独自 `fetch` は持たず、`const { students } = useAuth()` 経由でデータを直接受領して描画する。
3. **`App.tsx`**:
   - `loading === true` の間は全画面オーバーレイで描画をロックし、認証状態確定前の画面チラつきを遮断する。

---

## 4. 本番デプロイ仕様（Vercel）

- プロジェクト名: `school-bus-app`
- デプロイコマンド:
  ```bash
  npx vercel --prod
  ```
- GitHub 連携リポジトリ: `https://github.com/yagijinai/school-bus-app.git` (ブランチ: `main`)
